{
  "name": "business-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter wagebook dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "test:golden": "pnpm --filter @plutus/compliance run test:golden",
    "test:e2e": "pnpm --filter wagebook run test:e2e"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
